apiVersion: v2
name: kube-prometheus-stack
description: Prometheus + Grafana + Alertmanager - local lab wrapper chart
type: application
version: 1.0.0
dependencies:
  - name: kube-prometheus-stack
    version: "82.12.0"
    repository: https://prometheus-community.github.io/helm-charts
